<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dualion.view"
    android:versionCode="1"
    android:versionName="1.0" >

    <!--<uses-sdk android:minSdkVersion="8" />-->

    <!--  Internet Permissions -->
    <!--<uses-permission android:name="android.permission.INTERNET" />-->
    <uses-permission android:maxSdkVersion="19" android:name="android.permission.INTERNET"/>

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.dualion.view.MainScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All Product Activity -->
        <activity
            android:launchMode="singleTop"
            android:name="com.dualion.view.SearchActivity"
            android:label="All Products" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name="com.dualion.view.NewProductActivity"
            android:label="Add New Product" >
        </activity>

        <!-- Edit Product Activity -->
        <activity
            android:name="com.dualion.view.EditProductActivity"
            android:label="Edit Product" >
        </activity>
    </application>
</manifest>
